"""Tessera batch worker.

Run modes:
- HTTP server (Cloud Run service):    python -m tessera_worker.main
- One-shot job (Cloud Run Job):       python -m tessera_worker.jobs.<name>
"""

__version__ = "0.1.0"
