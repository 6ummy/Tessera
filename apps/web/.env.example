# Copy to .env.local and fill. .env.local is gitignored.
# (Next.js convention: .env.local is the active local-dev file, never committed.)
#
# For Vercel deploys, set these same vars in:
#   Vercel project → Settings → Environment Variables
#   (apply to Production / Preview / Development as needed)

# Vercel Cron auth — must match the value set in Vercel project env.
# Generate a random secret: `openssl rand -hex 32` or any password manager.
CRON_SECRET=

# Worker webhook (Phase A: leave blank → cron returns noop)
# Phase B Week 2: set to the deployed Cloud Run URL.
WORKER_WEBHOOK_URL=
# WORKER_WEBHOOK_SECRET is the legacy bearer auth — kept as a fallback
# during the IAM rollout (PR feat/infra/cloud-run-iam-auth, 2026-06-09).
# Will be removed after the IAM path is stable for 1–2 weeks.
WORKER_WEBHOOK_SECRET=

# GCP service-account key for Cloud Run identity-token minting.
# Base64-encoded contents of tessera-vercel@…iam.gserviceaccount.com key
# JSON (see docs/runbooks/cloud-run-iam-auth.md for the one-time setup).
# When set, Edge proxies sign requests with an OIDC token addressed to the
# worker; the worker's --no-allow-unauthenticated config validates the
# IAM membership and accepts the call.
GCP_SA_KEY_B64=

# Observability — Sentry DSN for the Next.js project (tessera-web).
# SENTRY_DSN          → used by server-side (Edge + Node runtimes)
# NEXT_PUBLIC_SENTRY_DSN → exposed to the browser bundle (must start with NEXT_PUBLIC_)
# Both should hold the SAME web DSN. Get it from sentry.io → Projects → tessera-web.
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=

# ─── Firebase Auth (Phase D, Google SSO) ───────────────────────────────
# These are the PUBLIC web-app config values (safe to ship to the browser;
# access is gated by Firebase Auth rules + authorized domains, not secrecy).
# Leave blank and the app runs the pre-auth pilot experience (no sign-in
# button). Fill all four to turn on Google SSO. One-time operator setup:
# docs/runbooks/firebase-auth.md.
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# Neon connection for the USER layer (server-only, NOT NEXT_PUBLIC).
# The /api/auth/sync route uses this to upsert the `users` row on login.
# Same Neon database as the worker's DATABASE_URL — copy that value.
# Firebase ID tokens are verified with `jose` against Google's public JWKS,
# so NO firebase-admin service-account secret is needed.
DATABASE_URL=
